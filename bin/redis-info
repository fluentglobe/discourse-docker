#!/bin/bash
# Display $REDIS_URL for running fluentglobe/redis

set -e
. etc/env

ID=$(sudo docker ps | grep fluentglobe/redis:2.6 | awk '{print $1}')
REDIS_PORT=$(sudo docker port $ID 6379)

export REDIS_PROVIDER_URL="redis://user@$HOSTIP:$REDIS_PORT"
